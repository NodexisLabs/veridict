# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# tooling / experiment artifacts (not part of the package)
.deepeval/
.deepeval

# local throwaways: red-team scripts, screenshot renderers, generated reports, launch notes
# (root-anchored so it never catches package dunders like veridict/__init__.py)
/_*.py
/_*.ps1
*.png
*.html
LAUNCH_POST.md
